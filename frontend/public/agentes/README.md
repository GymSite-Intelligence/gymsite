# Ícones dos 5 especialistas (app)

**Canônico (fonte de arte):**  
`C:\Users\marce\gymsite_intelligence\docs\produto\brand\icons\` (`mascote-*.png`)

**Deploy site (mesmo visual):**  
`gym-insight-hub/public/agentes/{mercado,tecnico,regulatorio,arquiteto,engenheiro}.png`

**App logado:**  
`gymsite/frontend/public/agentes/*.png` — cópia **byte-a-byte** do hub (paridade site).

Não usar SVG line-art (`agent-svg/*.svg`) no rail/avatar dos 5 especialistas do consultor.  
SVGs ficam só para ícones de pipeline/setor legados se necessário.

Ao atualizar mascote: copiar de `brand/icons` → hub `public/agentes` (com nomes do site) → `frontend/public/agentes`.
