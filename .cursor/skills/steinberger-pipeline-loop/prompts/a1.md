# /goal — A1 GeoScout (listing + MRLR)

Faça o arquivo `agents/a1_geoscout.py` cumprir estritamente o contrato definido em `tests/test_a1.py`.

## CONDIÇÃO DE PARADA (VERIFIER GATE)

```powershell
c:\Users\marce\gymsite_intelligence\.venv\Scripts\python.exe -m pytest tests/test_a1.py -q --tb=short -x
```

O ciclo só encerra quando **4 testes** passarem com exit code 0.

Flags ([pytest usage](https://docs.pytest.org/en/stable/how-to/usage.html) / [failures](https://docs.pytest.org/en/stable/how-to/failures.html) / [exit codes](https://docs.pytest.org/en/stable/reference/exit-codes.html)):
- `-q` — menos ruído na janela
- `--tb=short` — assert + linha, sem stack enorme
- `-x` — para na 1ª falha (uma correção por iteração)
- exit `0` = /goal ok · `1` = falha · `2`/`4` = ambiente/comando

## REGRAS INEGOCIÁVEIS

1. PROIBIDO alterar `tests/test_a1.py`.
2. A1 = `BaseAgent` / `GeoScoutAgent` **sem** `model`.
3. `name` = `"GeoScout"`.
4. **NÃO** chamar `analisar_pontos_comerciais_completo`.
5. Fonte = `buscar_candidatos_listing_mrlr`: listing SearchAPI filtrado + MRLR.
6. Grava **as duas** chaves com o mesmo dict; `status=ok|ok_vazio`.
7. Candidato tem `qualidade_sinal=direto-listing-bairro`; lista vazia não recebe POI.
8. Uma modificação pontual por iteração.

## CICLO

1. [OBSERVAR] Rodar pytest; ler falhas.
2. [AGIR] Editar só `agents/a1_geoscout.py`.
3. [VERIFICAR] Rodar pytest de novo.
4. [DECIDIR] 4/4 OK → encerrar; senão repetir (máx 5).
