# /goal — A4 FinancialEstimator (viabilidade + MRLR)

Faça o arquivo `agents/a4_financial_estimator.py` cumprir estritamente o contrato definido em `tests/test_a4.py`.

## CONDIÇÃO DE PARADA (VERIFIER GATE)

```powershell
python -m pytest tests/test_a4.py -q --tb=short -x
```

O ciclo só encerra quando **7 testes** passarem com exit code 0.

Flags ([pytest usage](https://docs.pytest.org/en/stable/how-to/usage.html) / [failures](https://docs.pytest.org/en/stable/how-to/failures.html) / [exit codes](https://docs.pytest.org/en/stable/reference/exit-codes.html)):
- `-q` — menos ruído na janela
- `--tb=short` — assert + linha, sem stack enorme
- `-x` — para na 1ª falha (uma correção por iteração)
- exit `0` = /goal ok · `1` = falha · `2`/`4` = ambiente/comando

## REGRAS INEGOCIÁVEIS

1. PROIBIDO alterar `tests/test_a4.py`.
2. A4 = `BaseAgent` / `FinancialEstimatorAgent` **sem** `model`.
3. `name` = `"FinancialEstimator"`.
4. Fonte = macro async `analise_financeira_a4_completo` (3 cenários low/mid/premium).
5. **Aluguel = MRLR Tier 0** (`aluguel_mrlr.py`), determinístico, dentro da macro —
   NUNCA SearchAPI, NUNCA A7, NUNCA `bundle.aluguel_portais` (canônico conferencia-fontes).
6. `justificativa` por TEMPLATE dos campos estruturados (modelo/margem/payback/gênero); zero LLM.
7. Grava **as duas** chaves com o mesmo dict: `analise_financeira_pronto` (A6 lê os números) + `analise_financeira` (legado).
8. Degrada limpo: macro falha/não-dict → `score_viabilidade = None`, pipeline não cai.
9. Uma modificação pontual por iteração.

## CICLO

1. [OBSERVAR] Rodar pytest; ler falhas.
2. [AGIR] Editar só `agents/a4_financial_estimator.py`.
3. [VERIFICAR] Rodar pytest de novo.
4. [DECIDIR] 7/7 OK → encerrar; senão repetir (máx 5).
