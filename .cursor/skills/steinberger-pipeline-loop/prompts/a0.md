# /goal — A0 ContextBuilder

Faça o arquivo `agents/a0_context_builder.py` cumprir estritamente o contrato definido em `tests/test_a0.py`.

## CONDIÇÃO DE PARADA (VERIFIER GATE)

```powershell
c:\Users\marce\gymsite_intelligence\.venv\Scripts\python.exe -m pytest tests/test_a0.py -q --tb=short -x
```

O ciclo só encerra quando **4 testes** passarem com exit code 0.

Flags: `-q` (menos ruído) · `--tb=short` (assert direto) · `-x` (1 falha = para). Exit `0` = done; `1` = iterate; `2`/`4` = fix ambiente.

## REGRAS INEGOCIÁVEIS

1. PROIBIDO alterar `tests/test_a0.py`.
2. CONTRATO BUNDLE-ONLY — `context_builder_agent.tools` NÃO pode incluir:
   - `rodar_deep_research`
   - `rodar_kimi_research`
   - `google_search`
   - `buscar_conhecimento`
3. `after_agent_callback` = exatamente `a0._a0_override_cnpj_numeros`.
4. `output_key` = exatamente `"market_context"`.
5. Uma modificação pontual por iteração. Sem refator não relacionado.

## CICLO

1. [OBSERVAR] Rodar pytest; ler falhas.
2. [AGIR] Editar só `agents/a0_context_builder.py`.
3. [VERIFICAR] Rodar pytest de novo.
4. [DECIDIR] 4/4 OK → encerrar + resumo do diff; senão repetir (máx 5).
