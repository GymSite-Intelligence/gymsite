# /goal — A9 PositioningStrategist (ERRC determinística)

Faça o arquivo `agents/a9_positioning_strategist.py` cumprir estritamente o contrato definido em `tests/test_a9.py`.

## CONDIÇÃO DE PARADA (VERIFIER GATE)

```powershell
C:\Users\marce\gymsite_intelligence\.venv\Scripts\python.exe -m pytest tests/test_a9.py -q --tb=short -x
```

O ciclo só encerra quando **7 testes** passarem com exit code 0.

Flags ([pytest usage](https://docs.pytest.org/en/stable/how-to/usage.html) / [failures](https://docs.pytest.org/en/stable/how-to/failures.html) / [exit codes](https://docs.pytest.org/en/stable/reference/exit-codes.html)):
- `-q` — menos ruído na janela
- `--tb=short` — assert + linha, sem stack enorme
- `-x` — para na 1ª falha (uma correção por iteração)
- exit `0` = /goal ok · `1` = falha · `2`/`4` = ambiente/comando

## REGRAS INEGOCIÁVEIS

1. PROIBIDO alterar `tests/test_a9.py`.
2. A9 = `BaseAgent` / `PositioningStrategistAgent` **sem** `model` (determinístico, sem LLM).
3. `name` = `"PositioningStrategist"`; `after_agent_callback` = `_a9_after_agent_callback`.
4. ERRC (eliminar/reduzir/aumentar/criar) + veredito + ticket + mapa de serviços nascem
   de `_errc_deterministica`: headroom de renda (IBGE Censo 2022) + oferta REAL dos
   concorrentes (planos + modalidades + IG). Zero palpite do LLM.
5. Base de penetração = a PRAÇA inteira com dedupe por nome (`_concorrentes_para_oferta`):
   detalhados + academias_analisadas + top_independentes + brutos A3a.
6. GAP só existe quando NINGUÉM da praça anuncia o serviço (`_gaps_reais`); mercado coberto
   → foco em AUMENTAR/REDUZIR, nunca CRIAR serviço inventado.
7. Sem `cidade`/`bairro` resolvíveis → veredito `INDETERMINADO` e ressalva idempotente
   (`_emendar_ressalva`); pipeline não cai (`_run_async_impl` nunca propaga exceção).
8. Todo número exibido carrega carimbo (valor · base · fonte · janela).
9. Uma modificação pontual por iteração.

## CICLO

1. [OBSERVAR] Rodar pytest; ler a 1ª falha.
2. [AGIR] Editar só `agents/a9_positioning_strategist.py`.
3. [VERIFICAR] Rodar pytest de novo.
4. [DECIDIR] 7/7 OK → encerrar; senão repetir (máx 5).
