# /goal — A3 CompetitorSearch (A3a) + CompetitorAnalysis (A3b)

Faça os arquivos `agents/a3a_competitor_search.py` e `agents/a3b_competitor_analysis.py`
cumprirem estritamente o contrato definido em `tests/test_a3.py`.

## CONDIÇÃO DE PARADA (VERIFIER GATE)

```powershell
python -m pytest tests/test_a3.py -q --tb=short -x
```

O ciclo só encerra quando **8 testes** passarem com exit code 0.

Flags ([pytest usage](https://docs.pytest.org/en/stable/how-to/usage.html) / [failures](https://docs.pytest.org/en/stable/how-to/failures.html) / [exit codes](https://docs.pytest.org/en/stable/reference/exit-codes.html)):
- `-q` — menos ruído na janela
- `--tb=short` — assert + linha, sem stack enorme
- `-x` — para na 1ª falha (uma correção por iteração)
- exit `0` = /goal ok · `1` = falha · `2`/`4` = ambiente/comando

## REGRAS INEGOCIÁVEIS

1. PROIBIDO alterar `tests/test_a3.py`.
2. A3 = par determinístico `BaseAgent` **sem** `model`. O LlmAgent `a3_competitor_intel.py`
   (CompetitorIntel) está MORTO — não religar.
3. Nomes: `"CompetitorSearch"` (A3a) e `"CompetitorAnalysis"` (A3b).
4. A3a = `analisar_concorrentes_a3a_completo` → grava `concorrentes_brutos`.
5. A3b = `analisar_concorrentes_completo` → grava `inteligencia_competitiva` + `oferta_concorrentes`;
   sintetiza posicionamento/resumo por TEMPLATE (zero número novo).
6. Filtro autoritativo do envelope = **tipo/status**; NUNCA gate de string de bairro
   (canônico §9: inclusão = R=1000m do centróide, em `buscar_academias`).
7. Degrada limpo: falha da macro nunca derruba o pipeline (A3a → lista vazia; A3b → envelope seguro score 7.0).
8. Uma modificação pontual por iteração.

## CICLO

1. [OBSERVAR] Rodar pytest; ler falhas.
2. [AGIR] Editar só os agentes A3a/A3b.
3. [VERIFICAR] Rodar pytest de novo.
4. [DECIDIR] 8/8 OK → encerrar; senão repetir (máx 5).
