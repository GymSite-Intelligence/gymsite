# /goal — A5 ContactHunter (prospecção determinística)

Faça o arquivo `agents/a5_contact_hunter.py` cumprir estritamente o contrato
definido em `tests/test_a5.py`.

## CONDIÇÃO DE PARADA (VERIFIER GATE)

```powershell
c:\Users\marce\gymsite_intelligence\.venv\Scripts\python.exe -m pytest tests/test_a5.py -q --tb=short -x
```

O ciclo só encerra quando **5 testes** passarem com exit code 0.

- `-q` — menos ruído na janela
- `--tb=short` — assert e linha, sem stack extenso
- `-x` — para na primeira falha
- exit `0` = meta atingida; `1` = falha; `2`/`4` = ambiente ou comando

## REGRAS INEGOCIÁVEIS

1. PROIBIDO alterar `tests/test_a5.py`.
2. A5 = `BaseAgent` / `ContactHunterAgent` sem `model`.
3. `name` = `"ContactHunter"`.
4. A5 pertence à prospecção e fica fora do pipeline de viabilidade.
5. Executa `gerar_contato_decisor_completo` em thread com o state real.
6. Grava somente `contato_decisor` no `state_delta`.
7. Resultado não-dict vira `{}`.
8. Falha da macro gera `erro` e não interrompe o fluxo.
9. Uma modificação pontual por iteração.

## CICLO

1. [OBSERVAR] Rodar o verifier e ler a primeira falha.
2. [AGIR] Editar somente `agents/a5_contact_hunter.py`.
3. [VERIFICAR] Rodar o verifier novamente.
4. [DECIDIR] 5/5 OK encerra; caso contrário, repetir por no máximo 5 iterações.
