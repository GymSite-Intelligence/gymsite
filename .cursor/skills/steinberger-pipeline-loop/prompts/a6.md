# /goal — A6 ReportConsolidator (narrativa com guardrails)

Faça o arquivo `agents/a6_report_consolidator.py` cumprir estritamente o
contrato definido em `tests/test_a6.py`.

## CONDIÇÃO DE PARADA (VERIFIER GATE)

```powershell
c:\Users\marce\gymsite_intelligence\.venv\Scripts\python.exe -m pytest tests/test_a6.py -q --tb=short -x
```

O ciclo só encerra quando **7 testes** passarem com exit code 0.

- `-q` — menos ruído na janela
- `--tb=short` — assert e linha, sem stack extenso
- `-x` — para na primeira falha
- exit `0` = meta atingida; `1` = falha; `2`/`4` = ambiente ou comando

## REGRAS INEGOCIÁVEIS

1. PROIBIDO alterar `tests/test_a6.py`.
2. A6 pode usar LLM somente para narrativa; número vem de tool, banco ou state.
3. `name` = `"ReportConsolidator"` e `output_key` = `"relatorio_md"`.
4. Preservar `_a6_precompute_callback`, `_a6_before_model_callback` e
   `_a6_after_agent_callback`.
5. A4 é lido prioritariamente de `analise_financeira_pronto`.
6. Top3 é determinístico: listing A1 + MRLR + 35% geo / 65% payback A4.
7. Preço bruto do anúncio não alimenta aluguel nem decisão.
8. O alinhador substitui narrativa divergente pelos dados estruturados.
9. Ausência de candidato é declarada honestamente; não criar âncora ou imóvel.
10. Uma modificação pontual por iteração.

## CICLO

1. [OBSERVAR] Rodar o verifier e ler a primeira falha.
2. [AGIR] Editar somente `agents/a6_report_consolidator.py`.
3. [VERIFICAR] Rodar o verifier novamente.
4. [DECIDIR] 7/7 OK encerra; caso contrário, repetir por no máximo 5 iterações.
