# /goal — A2 DemoAnalyst (IBGE determinístico)

Faça o arquivo `agents/a2_demo_analyst.py` cumprir estritamente o contrato definido em `tests/test_a2.py`.

## CONDIÇÃO DE PARADA (VERIFIER GATE)

```powershell
python -m pytest tests/test_a2.py -q --tb=short -x
```

O ciclo só encerra quando **6 testes** passarem com exit code 0.

Flags ([pytest usage](https://docs.pytest.org/en/stable/how-to/usage.html) / [failures](https://docs.pytest.org/en/stable/how-to/failures.html) / [exit codes](https://docs.pytest.org/en/stable/reference/exit-codes.html)):
- `-q` — menos ruído na janela
- `--tb=short` — assert + linha, sem stack enorme
- `-x` — para na 1ª falha (uma correção por iteração)
- exit `0` = /goal ok · `1` = falha · `2`/`4` = ambiente/comando

## REGRAS INEGOCIÁVEIS

1. PROIBIDO alterar `tests/test_a2.py`.
2. A2 = `BaseAgent` / `DemoAnalystAgent` **sem** `model` (zero token de LLM).
3. `name` = `"DemoAnalyst"`.
4. Fonte = macro `analise_demografica_completa` (IBGE Censo 2022), rodada em thread.
5. Grava `analise_demografica` no `state_delta` com `insights` derivados em Python.
6. Insights são templates determinísticos dos números (público/renda/score), nunca LLM.
7. Degrada limpo: qualquer falha → `analise_demografica.score_demografico = None`, pipeline não cai.
8. Enriquecimentos (perfil sexo×idade, densidade setor) degradam em `try/except` sem derrubar o passo.
9. Uma modificação pontual por iteração.

## CICLO

1. [OBSERVAR] Rodar pytest; ler falhas.
2. [AGIR] Editar só `agents/a2_demo_analyst.py`.
3. [VERIFICAR] Rodar pytest de novo.
4. [DECIDIR] 6/6 OK → encerrar; senão repetir (máx 5).
