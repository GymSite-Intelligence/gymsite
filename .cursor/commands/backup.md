---
name: backup
description: "Backup Supabase + inventário secrets (sem valores)"
---

# /backup

Seguir **à letra** o workflow:

`.agent/workflows/backup.md`

PowerShell. Dump `gymsite`/`shared` — nunca valores de secrets no zip.
